#!/bin/sh

if [ "${1##*.}" = "avi" ]; then
	options="$options -input conf=input.conf"
fi

#exec mplayer -vf pp=fd "$@" $options  && sleep 1

file="$1"
shift
short="${file##*/}"

if [ -f "$short" ]; then
	file="$short"
fi

exec mplayer "$file" "$@" $options  && sleep 1
